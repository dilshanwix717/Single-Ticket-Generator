# HTTP
PORT=3000

# PostgreSQL (used by Nest TypeORM and docker-compose db service)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=tickets

# Set to "false" in production if you manage schema with migrations instead of sync
TYPEORM_SYNC=true
TYPEORM_LOGGING=false
